spring:
  profiles:
    include:
      - dev
      #- security
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8080
  servlet:
    context-path: /college-mate
